# 05_语言规范校对 · 波次 3 反向审计报告

> 审计时间：2026-08-28 · 审计类型：基线能力反向审计（升级版 vs 基线）
> 依据：能力不退化铁则 + 基线锚定铁则 + 熔断机制
> 审计范围：`src/paeg_lang_style/` 全部 9 个模块 + `data/` 3 个 JSON + `tests/` 6 个测试文件（只读，未改任何代码）

## 一、基线锚定状态（锁定 commit + L-01~L-12 清单）

- **基线版本**：PAEG 主项目语言规范模块 commit `ed895df`（2026-08-23 快照）
- **基线来源**：`language_refiner.py` / `ai_taste_detector.py` / `services/lang_gate.py` / `services/polish.py` / `weil_corpus.json` / `data/forbidden_words.json`
- **当前工作区锁定**：分支 `main`，HEAD `1aa731b`（docs 为主，代码自 `e7eb61f` MCP server 化后无代码提交）；存在 2 个未提交 docs 改动（`04_审计报告_波次1.md`、`docs/README.md`），本审计不触碰。
- **清单来源**：`docs/03_基线能力清单.md` §二（L-01~L-12，共 12 项）

| 编号 | 能力名称 | 能力描述（摘要） |
|---|---|---|
| L-01 | AI 腔词检测 | 577 项 AI 腔模式子串检测，返回命中列表 |
| L-02 | AI 味多维信号检测 | burstiness/marker_density/三点列举/破折号/段落对称度 → ai_likelihood |
| L-03 | Self-Refine 多轮语言矫正 | 薇依语料 few-shot + 反馈 + LLM 改写 + 复检循环（max_rounds=2） |
| L-04 | 省略句/无主句检测 | 句首动词/无主语短语/动宾搭配/悬空宾语/介词规范/复合句缺主语 |
| L-05 | 高频词重复检测 | 200 字窗口内核心词 ≥3 次触发同义词轮换建议 |
| L-06 | 病句确定性修正 | fix_known_gaffes：悬空"听着你"正则修正，保护合法搭配 |
| L-07 | 统一语言守门入口 | lang_gate_content（L0-0+L0+L2+收口）/ lang_gate_short 快路径 |
| L-08 | 全局守门兼容入口 | polish_text：AI 味/省略句/动宾触发 → refine，异常静默回退原文 |
| L-09 | 薇依语料 few-shot | weil_corpus.json 前 6 条进 system prompt，风格锚定 |
| L-10 | 外部禁词可配置 | forbidden_words.json 三类禁词合并进 AI_TELLS，无需改代码 |
| L-11 | 静默降级安全 | 全部矫正链路 try/except 包裹，异常回退原文 |
| L-12 | 可移植注入接口 | chat_fn 注入式，LLM 调用可解耦替换 |

## 二、逐项映射审计表

| 编号 | 基线能力项 | 落地文件:行 | 测试证据(测试文件::测试函数) | 判定 |
|---|---|---|---|---|
| L-01 | AI 腔词检测 | `forbidden.py:32-118`（`_BUILTIN_FORBIDDEN` 552 项）、`forbidden.py:167-171`（`detect`）、`refiner.py:75-77`（`detect_ai_tells`） | `test_plugin.py::TestForbiddenWords::test_detect_hits`；`test_parity.py::test_ai_taste_parity_with_paeg`（PAEG 目录存在，实际运行非 skip） | 保留 |
| L-02 | AI 味多维信号检测 | `ai_taste.py:148-159`（`AITasteSignals` 5 维）、`ai_taste.py:225`（`detect_ai_taste`）、`176/187/195/208/213`（5 个度量函数） | `test_plugin.py::TestDetectAITaste::test_ai_slang_text`、`::test_human_text`；`test_mcp_server.py::test_check_ai_taste` | 保留 |
| L-03 | Self-Refine 多轮语言矫正 | `refiner.py:88`（`refine`，`max_rounds=2`）、`refiner.py:137-154`（改写+复检循环）、`refiner.py:160-210`（`_get_feedback`） | `test_plugin.py::TestRefiner::test_refine_ai_text_triggers_chat`、`::test_refine_rules_without_llm_call` | 保留 |
| L-04 | 省略句/无主句检测 | `rules.py:158-228`（`check_ellipsis`）；规则表 `46-70/76-93/99-106/112-117/123-126/132-140/146-155` | `test_plugin.py::TestCheckEllipsis`（6 用例）；`test_parity.py::test_samples_cover_8_rules`、`::test_ellipsis_parity_with_paeg` | 保留 |
| L-05 | 高频词重复检测 | `refiner.py:221-257`（`_check_word_repetition`）、`refiner.py:214-219`（触发词/窗口 200/阈值 3） | **无测试断言**（基线"单测"未迁移） | 保留（实现完整，测试缺口） |
| L-06 | 病句确定性修正 | `rules.py:31-40`（`_GAFFE_FIX_RULES` 4 条）、`rules.py:231-247`（`fix_known_gaffes`） | `test_plugin.py::TestFixKnownGaffes`（6 用例，原 `test_round19_gaffe_fix` 改名）；`test_parity.py::test_gaffes_parity_with_paeg` | 保留 |
| L-07 | 统一语言守门入口 | `gate.py:29-68`（`gate_content`，原 `lang_gate_content` 改名）、`gate.py:71-81`（`gate_short`，原 `lang_gate_short` 改名） | `test_plugin.py::TestGate`（4 用例） | 保留 |
| L-08 | 全局守门兼容入口 | **缺失**：无 `polish.py` 模块、无 `polish_text` 函数；仅存 `gate.py:30/42/50-53/76-78` 的 `polish_fn` 注入钩子（默认 None=跳过） | **无测试**（基线 `test_contracts` 不存在） | **缺失** |
| L-09 | 薇依语料 few-shot | `refiner.py:63-72`（`_load_corpus`，默认路径 `65-66` 指向不存在的 `src/paeg_lang_style/data/weil_corpus.json`）、`refiner.py:269`（`self.corpus[:6]`）；语料文件在 `data/weil_corpus.json`（10 条） | **无测试断言**；运行时验证 `make_refiner` 默认加载 `len(r.corpus)==0` | **退化** |
| L-10 | 外部禁词可配置 | `forbidden.py:146-164`（`load_json`，`160` 行仅读 `extra_forbidden`/`ai_tells_extra` 两键，跳过 `pseudo_empathy_verbs`）；`refiner.py:57` 默认调用；文件 `data/forbidden_words.json`（extra 18 / pseudo_empathy 12 / ai_tells_extra 7） | `test_plugin.py::TestForbiddenWords::test_load_json`（断言 `added>=0`，弱）；运行时验证默认 `load_json()`→`added=0` | **退化** |
| L-11 | 静默降级安全 | `gate.py:53-55/58-65/77-80`、`refiner.py:105-109/139-144/149-154/163-176`、`rules.py:241-247`、`forbidden.py:157-158`（try/except 包裹完整） | **无异常注入测试**（基线"异常注入测试"未迁移）；相关 `test_rule_registry.py::test_load_bad_json_keeps_rules` | 保留（实现完整，测试缺口） |
| L-12 | 可移植注入接口 | `refiner.py:45-52`（`chat_fn=None` 抛 `TypeError` fail-fast，比基线 `subagents._safe_chat` 缺省更强）、`refiner.py:310-312`（`make_refiner`） | `test_plugin.py::TestRefiner::test_chat_fn_required`、`::test_make_refiner_injects` | 保留 |

**统计**：保留 9 项（L-01/02/03/04/05/06/07/11/12）· 缺失 1 项（L-08）· 退化 2 项（L-09/L-10）· 未映射 0 项。

## 三、测试基线锁定（pytest 输出摘要 + 通过数）

```
$ python -m pytest tests/ -q -p no:cacheprovider -rs
........................................................................ [ 86%]
...........                                                              [100%]
83 passed in 15.83s
```

- **通过数：83 passed / 0 failed / 0 skipped / 0 error**
- **关键佐证**：`fastmcp 3.4.6` 已安装 → 8 个 MCP server 测试实际运行；PAEG 原实现目录 `D:/wbo-workspace/paeg_project/05_实现原型` 存在 → 3 个行为一致性测试（`test_gaffes_parity_with_paeg` / `test_ellipsis_parity_with_paeg` / `test_ai_taste_parity_with_paeg`）**实际运行且通过**（非 skip），证明 L-01/L-02/L-04/L-06 与 PAEG 原实现字符串/命中数/verdict 零漂移。
- **测试分布**：test_plugin 31 + test_parity 4 + test_rule_registry 18 + test_mcp_server 8 + test_general_rules 14 + test_adverbial_rule 8 = **83**。
- **注意**：全部通过 ≠ 全部有覆盖。L-05、L-09、L-11 在 83 项测试中无任何断言项（见 §六）。

## 四、熔断检查

| 熔断条件 | 状态(触发/未触发) | 证据 |
|---|---|---|
| 缺失熔断：任何基线项 ≥1 缺失 | **触发** | L-08 `polish_text` 函数/`polish.py` 模块整体缺失（grep 全库仅存 `gate.py` 的 `polish_fn` 注入钩子，无实现；模块清单 8 个无 `polish`） |
| 退化熔断：任何应保留项退化 | **触发** | L-09 语料默认路径失效（`refiner.py:65-66` 指向 `src/paeg_lang_style/data/`，实测 `src/paeg_lang_style/data/` 不存在，默认加载 `len(corpus)==0`）；L-10 默认路径失效（实测 `load_json()`→`added=0`）+ 三类禁词变两类（`forbidden.py:160` 跳过 `pseudo_empathy_verbs`，净丢 1 词"被听见"） |
| 覆盖熔断：覆盖率 <100%（未映射项） | 未触发 | 12/12 项全部映射到 file:line（0 未映射） |

## 五、放行判定

**熔断（不放行）。**

- 缺失熔断触发（L-08）→ 直接熔断，无需进入放行。
- 退化熔断触发（L-09、L-10）→ 违反「能力不退化铁则」。
- 覆盖熔断未触发（12/12 映射，无未映射项）。

结论：当前代码**未**实现 100% 基线能力保留。波次 2 方案设计（`docs/05_技术架构设计.md` §四「原样保留」）与波次 1 对标表 §四「原样保留」对 L-08/L-09/L-10 的乐观判断与代码现状不符，需进入波次 3 补缺 backlog。

## 六、缺失/退化清单（供波次 3 补缺 backlog，逐项含 TDD 建议）

### 🔴 缺失（1 项）

**L-08 全局守门兼容入口（`polish_text`）**
- 现状：`services/polish.py::polish_text` 整体未迁移。`gate.py` 仅保留 `polish_fn: Optional = None` 注入参数，缺省即跳过 L0 polish 层；"AI 味/省略句/动宾触发条件 → refine + 异常静默回退"的兼容入口语义丢失（部分被 `refiner.refine` 内联消化，但无独立入口函数、无 `test_contracts`）。
- TDD 建议：
  1. 红：`tests/test_polish.py::test_polish_text_triggers_refine`——给定含"我在这里听着你。+ AI 腔"文本，`polish_text(text, context)` 触发 refine 且输出不含"听着你"；`::test_polish_text_silent_fallback`——mock refine 抛异常，断言 `polish_text(...) == 原文`。
  2. 绿：实现 `polish_text`（AI 味/省略句/动宾检测 → 条件 refine → try/except 回退原文），并纳入 `__init__.py` 导出。

### 🟡 退化（2 项）

**L-09 薇依语料 few-shot（默认路径失效）**
- 现状：语料文件已从包内移至仓库根 `data/weil_corpus.json`（10 条），但 `refiner.py:65-66` 默认路径仍拼 `src/paeg_lang_style/data/weil_corpus.json`（不存在）；`pyproject.toml:29-30` 的 `package-data` 也指向包内 `data/*.json`（打不进 wheel）。结果 `make_refiner(chat_fn=...)` 默认 `self.corpus=[]`，`_build_system` 落入"（无语料）"分支——few-shot 风格锚定在默认路径下失效。
- TDD 建议：
  1. 红：`tests/test_plugin.py::TestRefiner::test_corpus_loaded_by_default`——`make_refiner(chat_fn=...)` 后断言 `len(r.corpus) >= 6`，且 `r._build_system()` 含"语料原句 1"。
  2. 绿：二选一——(a) 将 `data/*.json` 移入 `src/paeg_lang_style/data/`（与 `package-data` 配置一致），或 (b) 修正 `_load_corpus`/`load_json`/`RuleRegistry.load` 默认路径指向仓库根 `data/`（并同步 `pyproject.toml`）。

**L-10 外部禁词可配置（默认路径失效 + 三类变两类）**
- 现状：两处退化叠加。
  - (a) 默认路径同 L-09 问题：`refiner.py:57` 调 `self.forbidden.load_json()` 无参 → 指向不存在的 `src/paeg_lang_style/data/forbidden_words.json` → 实测 `added=0`，外部禁词默认不加载。
  - (b) 三类合并缩水：`forbidden.py:160` 循环 `("extra_forbidden","ai_tells_extra")` 两键，`data/forbidden_words.json` 的第三类 `pseudo_empathy_verbs`（12 条）被跳过（净丢"被听见"等未内置词；实测显式路径加载 `added=10`，仅覆盖 extra 6 + ai_tells_extra 4 的净新词）。
- TDD 建议：
  1. 红：`tests/test_plugin.py::TestForbiddenWords::test_load_json_merges_three_categories`——显式路径加载后断言 `fb.detect("被听见") == ["被听见"]`（伪共情类净新词被合并）；`::test_load_json_default_path`——无参 `load_json()` 返回 `> 0`。
  2. 绿：`load_json` 读 `("extra_forbidden","pseudo_empathy_verbs","ai_tells_extra")` 三键 + 修正默认路径（与 L-09 同步修）。

### ⚪ 测试缺口（实现完整，基线验证用测试未迁移，2 项）

**L-05 高频词重复检测**
- 现状：`_check_word_repetition`（`refiner.py:221-257`）完整，运行时验证 `"重量"×3` 正确触发同义词轮换建议；但基线"单测"未迁移，83 项测试无一处断言。
- TDD 建议：`tests/test_plugin.py::TestRefiner::test_word_repetition`——`_check_word_repetition("重量 重量 重量 真实 看见 感受")` 返回含"重量"且"建议换用"的非空列表；窗口外（>200 字）不触发。

**L-11 静默降级安全**
- 现状：try/except 包裹完整（`gate.py`/`refiner.py`/`rules.py`/`forbidden.py`），但基线"异常注入测试"未迁移，83 项测试无异常注入用例（最接近的 `test_load_bad_json_keeps_rules` 只覆盖规则集 JSON 损坏）。
- TDD 建议：`tests/test_plugin.py::TestGate::test_silent_fallback_on_exception`——注入 `polish_fn`/`refiner` 使 `refine` 抛 `RuntimeError`，断言 `gate_content(text, refiner=r) == 原文`（不阻塞、不抛异常）。

## 七、补缺结果（2026-08-28 · 熔断解除 → 放行）

> 波次 3 补缺已按 §六 TDD 逐项完成，5 项缺口全部闭合：

| 项 | 缺口 | 修复 | TDD 证据 |
|---|---|---|---|
| L-08 | 缺失 `polish_text` | 新增 `src/paeg_lang_style/polish.py::polish_text(text, context, refiner)` + `__init__.py` 导出 | `test_polish.py::test_polish_text_triggers_refine` + `::test_polish_text_silent_fallback` |
| L-09 | 退化（语料默认路径失效） | `data/*.json` 移入 `src/paeg_lang_style/data/`，默认路径生效 | `test_plugin.py::TestRefiner::test_corpus_loaded_by_default` |
| L-10 | 退化（路径失效 + 三键缩水） | `forbidden.py:160` 读三键 `extra_forbidden/pseudo_empathy_verbs/ai_tells_extra` | `test_plugin.py::TestForbiddenWords::test_load_json_merges_three_categories` + `::test_load_json_default_path` |
| L-05 | 测试缺口 | 补 `test_word_repetition` | 断言"重量"×3 触发 / >200 字不触发 |
| L-11 | 测试缺口 | 补 `test_silent_fallback_on_exception` | 断言 refine 抛异常 → 返回原文 |

- **测试：83 → 90 passed**（+7 TDD 测试，0 failed）
- **熔断检查复评**：缺失 0 / 退化 0 / 覆盖 100%（12/12）→ 三条件均未触发
- **放行判定更新**：✅ **放行**（能力不退化铁则恢复成立，L-08/L-09/L-10 已恢复基线能力）
